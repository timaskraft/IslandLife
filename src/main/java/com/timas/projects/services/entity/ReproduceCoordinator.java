package com.timas.projects.services.entity;

import com.timas.projects.game.entity.Entity;
import com.timas.projects.services.random.RandomReproduceService;

import java.util.List;
import java.util.Set;

public class ReproduceCoordinator {

    private RandomReproduceService randomReproduceService;
    private ReproduceService reproduceService;


    void reproduce(List<Entity> entities)
    {
        //male
        //Map<KeyType, List<Entity>> groupedEntities = entities.stream()
        //       .collect(Collectors.groupingBy(Entity::getKey));



        //female


    }
}
